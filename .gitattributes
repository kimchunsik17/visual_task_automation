# 셸 스크립트는 항상 LF 로 체크아웃한다. Windows 개발 머신에서 core.autocrlf=true 로 받으면
# CRLF 가 섞이고, 그 파일을 리눅스 서버에서 실행하면 `bad interpreter: /usr/bin/env bash^M` 로
# 죽는다 — 배포 스크립트가 이 방식으로 깨지면 원인 찾기가 오래 걸린다.
*.sh text eol=lf
