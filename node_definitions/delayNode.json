{
  "type": "delayNode",
  "version": 1,
  "category": "logic",
  "display": {
    "label": "대기",
    "icon": "node-delay",
    "color": "#3b82f6"
  },
  "inputs": [
    {
      "name": "in",
      "dataType": "any"
    }
  ],
  "outputs": [
    {
      "name": "out",
      "dataType": "any"
    }
  ],
  "fields": [
    {
      "name": "seconds",
      "kind": "number",
      "label": "대기 시간 (초)",
      "placeholder": "10",
      "validation": [
        {
          "rule": "present",
          "message": "{node_id}(delayNode)에 seconds가 없다"
        },
        {
          "rule": "number",
          "min": 0,
          "message": "{node_id}(delayNode)의 seconds는 숫자여야 한다 (현재: {value_repr})",
          "minMessage": "{node_id}(delayNode)의 seconds는 0 이상이어야 한다 (현재: {value_repr})"
        }
      ]
    }
  ],
  "credentials": [],
  "capabilities": [],
  "sideEffect": "none",
  "executor": "delayNode",
  "mock": {},
  "llm": {
    "description": "지정한 시간만큼 대기 후 다음 노드로 진행. data.seconds(숫자).\n"
  }
}
